# Implementation plan and status

The `jamsession` tool is requisite groundwork for
[inter-agent communication](../README.md), so its implementation is tracked as
part of that RFD's combined, red-green TDD plan rather than separately.

See [Inter-agent communication: Implementation plan and status](../implementation.md)
— tool registration and `help` dispatch are its first steps.
